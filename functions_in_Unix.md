## creating a function
```bash
add_a_user()
{
  USER=$1
  PASSWORD=$2
  echo "Adding user $USER ..."
  echo "Adding user $PASSWORD..."
}
```

## Calling the function
```bash
add_a_user "RANGA" "NANDA"
```
